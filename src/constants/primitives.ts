import { Dimensions } from 'react-native';

export const APP_ACCESS_TOKEN = 'app-access-token';
export const APP_REFRESH_TOKEN = 'app-refresh-token';

export const APP_HORIZONTAL_PADDING = 32;
export const APP_VERTICAL_PADDING = 24;

export const SNACKBAR_TIMEOUT_MS = 5000;
export const SNACKBAR_MAX_SNACK = 3;

export const APP_ACTIVE_OPACITY = 0.7;

export const SCREEN_WIDTH = Dimensions.get('screen').width;
export const SCREEN_HEIGHT = Dimensions.get('screen').height;
